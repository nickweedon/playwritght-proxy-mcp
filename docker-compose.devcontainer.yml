services:
  devcontainer:
    build:
      context: .
      target: development
    env_file:
      - .env
    volumes:
      - .:/app:cached
      - /var/run/docker.sock:/var/run/docker.sock
      # Mount Claude Desktop MCP server logs (read-only)
      - C:\Users\nickw\AppData\Roaming\Claude\logs\mcp-server-playwright-proxy-mcp-docker.log:/app/logs/claude-desktop-mcp.log:ro
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
    command: sleep infinity
